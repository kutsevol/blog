stages:
    - pre-build
    - test
    - build

lint_python:
    stage: pre-build
    image: python:3.6.1
    script:
        - pip install flake8
        - make lint

tests:
    stage: test
    image: python:3.6.1
    script:
        - make install-requirements
        - make test

build_docker:
    stage: build
    script:
        - make docker-build
