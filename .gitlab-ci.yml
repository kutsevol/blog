stages:
    - pre-build
    - test

lint-python:
    stage: pre-build
    image: python:3.6.1
    script:
        - pip install flake8
        - make lint

tests:
    stage: test
    image: 3.6.1
    script:
        - make test
